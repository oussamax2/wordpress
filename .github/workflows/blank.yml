on: 
 pull_request:
    branches: [ master ]    
name: Publish Website
jobs:
  FTP-Deploy-Action:
    name: FTP-Deploy-Action
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2.1.0
      with:
        fetch-depth: 2
    - name: FTP-Deploy-Action
      uses: dorner/file-changes-action@v1.2.0
      with:
        ftp-server: ftp://ftp-itmaxtn.alwaysdata.net/www
        ftp-username: itmaxtn
        ftp-password: pirateX2@
